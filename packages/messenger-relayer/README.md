# messenger-relayer

This is the relayer using Scroll messenger API.

## Usage

To set up the development environment:

```bash
# install
yarn

# dev
yarn workspace messenger-relayer dev

# build
yarn build
```
